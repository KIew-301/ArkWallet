global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "arkwallet"
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials: change-me-metrics-api-key
    static_configs:
      - targets: ["app:5000"]
